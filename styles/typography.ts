import { css } from "styled-components";

const h1TextStyles = css`
  font-family: ${(p) => p.theme.FONTS.heading};
  font-size: 96px;
  font-style: normal;
  font-weight: 800;
  line-height: 90px;
  letter-spacing: -0.02em;
`;
